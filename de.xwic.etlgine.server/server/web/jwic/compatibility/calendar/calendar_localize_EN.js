/**
 * Localize the calendar. Must be invoked before the calendar.init function.
 */

// as the calendar is EN by default, no need to change the values.
// the file is just there to prevent errors on loading this locale.